dataModel:
    subProjectName: "js"

fileOutputs:
    files:
        - templatePath: "subproject.gradle.ftl"
          outputPath: "${subProjectName}/${subProjectName}.gradle"

        - templatePath: "js.gradle.ftl"
          outputPath: "js.gradle"
          reusable: true

    directories:
        - outputPath: "${subProjectName}/src"
        - outputPath: "${subProjectName}/public"
    
    classes:
          
    packages:
      
            