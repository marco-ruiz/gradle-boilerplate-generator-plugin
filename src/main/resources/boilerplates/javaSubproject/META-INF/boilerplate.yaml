dataModel:
    subProjectName: "common"

fileOutputs:
    files:
        - templatePath: "subproject.gradle.ftl"
          outputPath: "${subProjectName}/${subProjectName}.gradle"

        - templatePath: "java.gradle.ftl"
          outputPath: "java.gradle"
          reusable: true

    directories:
        - outputPath: "${subProjectName}/src/main/java"
        - outputPath: "${subProjectName}/src/test/java"
    
    classes:
          
    packages:
      